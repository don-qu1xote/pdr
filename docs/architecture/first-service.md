# Что обязан подобрать первый сервис

Фундамент строится без сервиса намеренно: домен, схема, механизмы и проверки не
нуждаются в поднятом процессе, а сервис, заведённый раньше них, оброс бы
случайными решениями. Плата за это — **долги**: куски, которые дописаны до
половины и ждут процесса, в котором смогут работать.

Долг, записанный в семи разных документах, — это долг, о котором забудут. Здесь
он собран в одном списке. Первый сервис — `PDR-SCHED-04` (`services/scheduling`),
и до его начала этот файл читают целиком.

Список закрытый и проверяемый: `scripts/check_debts.py` роняет сборку, если
какой-то документ говорит про первый сервис, а в этом списке его нет, — и если
здесь названа страница, которой больше не существует.

## Долги

| Долг | Что сделать | Чем проверить | Где записано |
| --- | --- | --- | --- |
| Компонент периодических заданий ни разу не запускался | Подключить `jobs::PeriodicJobComponentBase` в статический конфиг: `cluster`, `table: jobs_lock`, `task-processor`; завести первое задание | `make test-jobs` уже проверяет SQL-часть; добавить прогон задания через `service_client.run_task` | [ADR-0011](../adr/0011-single-jobs-on-distlock.md), [jobs.md](jobs.md) |
| `TESTPOINT` в фоновом воркере отсутствует | Поставить точку в `PeriodicJobComponentBase::Work()` после прогона и в обработчике очереди `PDR-SCHED-06` | Тест дожидается `testpoint(...).wait_call()` и НЕ содержит `sleep`; `scripts/check_testsuite.py` ловит `sleep` | [ADR-0012](../adr/0012-testsuite-on-pytest-userver.md), [testing.md](../testing.md) |
| Контур `pytest_userver` не заведён | `userver_testsuite_add_simple()`, `services/scheduling/tests/conftest.py` с `pytest_plugins = ['pytest_userver.plugins.postgresql']`, схема для testsuite из `db/migrations` | Четыре сценария из [testing.md](../testing.md): `service_client`, `mockserver`, `testpoint`, `@pytest.mark.config`; плюс chaos-прокси | [ADR-0012](../adr/0012-testsuite-on-pytest-userver.md), [testing.md](../testing.md) |
| Contract-набор хранилища гоняется только на фейке | Инстанцировать `repository_contract.hpp` против `PostgresTenantAwareRepository`, переопределив `PDR_CONTRACT_*` в `TYPED_UTEST_*` | Набор проходит на обеих реализациях; сломанный фейк по-прежнему роняет прогон | [testing.md](../testing.md) |
| Динамические конфиги: нет обновлятора, значит некому доставить значение | В статическом конфиге `dynamic-config`: `defaults-path`, `fs-cache-path`, `updates-enabled`. Журнал «было → стало» уже написан (подписка `dynamic_config::Diff` рядом с ключом), и проверка требует его у каждого ключа | Значение меняется без перезапуска на живом сервисе; источник недоступен — сервис поднялся на умолчаниях. Без сервиса то же самое проверено на штатных заглушках: цель `pdr_adapter_tests` | [registry.yaml](../../configs/dynamic/registry.yaml), [jobs.md](jobs.md) |
| «Кто изменил значение» взять негде | Решить, откуда берётся авторство: источник конфигов его не приносит, а штатная подписка знает только «было → стало» | Журнал отвечает на «оно само сломалось» за минуту, а не только на «изменилось» | этот файл, `git log --grep '\[PDR-CFG-01'` |
| Компонент `secdist` не подключён | Формат файла, права 0600, путь в окружении, реестр и сама проверка уже есть (`PDR-SEC-06`, `PDR-SEC-02`); в статическом конфиге завести `secdist` и `default-secdist-provider` с `missing-ok: false`, без `environment-secrets-key` | Старт без файла секретов падает внятно и называет путь | [deploy/README.md](../../deploy/README.md), `PDR-SEC-06` |
| `VerifySecrets` никто не зовёт: проверка написана, точки входа нет | Позвать `application::VerifySecrets(core::kEverySecret, SecdistSecretSource{...})` ДО подъёма остальных компонентов и упасть с её сообщением; сегодня она проверена на фейке, но не на настоящем файле | Старт с файлом без обязательного секрета падает и называет переменную; с полным файлом поднимается | [deploy/README.md](../../deploy/README.md), `PDR-SEC-02` |
| Тест на утечку секретов не гоняется | Прогнать список запрещённых подстрок по логам сервиса, по `/metrics` и по ручке статистики | `python3 scripts/check_secrets.py --forbidden` отдаёт список; тест берёт его оттуда, а не из головы | [check_secrets.py](../../scripts/check_secrets.py) |
| Ротация секретов не проверена | Штатное перечитывание файла — `update-period` у `secdist` (по умолчанию 10s); журнал «секрет заменён» — подпиской `Secdist::UpdateAndListen`, без значения в записи | Подмена файла подхватывается без перезапуска; в журнале имя раздела, а не значение | [deploy/README.md](../../deploy/README.md) |
| Кэш справочников не заведён | Когда появится справочная таблица и её контекст: `components::CachingComponentBase`, порт-читатель в `application` | Инвалидация из теста ручкой контура отдаёт новые данные | [ADR-0013](../adr/0013-standard-over-handmade.md) |
| Продуктовый поток некому писать: адаптера нет | Реализовать `ports::ProductEventStream` поверх `observability_product_event` и отдать контекстам `observability::Contract`; поля класть в `fields` как jsonb ШТАТНЫМ `formats::json`, а не сборкой строки | Сценарии из `libs/pdr-observability/tests/` проходят на настоящем адаптере тем же набором, что на фейке; выгрузка отдаёт записанное | [product-events.md](product-events.md), [open-questions.md](../product/open-questions.md) |
| Уборки продуктового потока нет — таблица растёт быстрее всех | Задание уборки по `recorded_at` со сроком из `PDR_PRODUCT_EVENTS`; индекс `observability_product_event_by_age` для него уже есть, SQL уже есть (`db/observability/prune.sql`) | `make product-events-prune DAYS=...` удаляет старые записи и не трогает свежие; план запроса проверяет `make test-plans` | [product-events.md](product-events.md), [registry.yaml](../../configs/dynamic/registry.yaml) |
| Уборки `jobs_effect` нет — таблица растёт | Задание уборки старых следов; индекс `jobs_effect_by_age` для него уже есть | Прогон уборки удаляет старые следы и не трогает свежие | [ADR-0011](../adr/0011-single-jobs-on-distlock.md), [jobs.md](jobs.md) |
| Счёт хеша пароля идёт на общем task_processor | Argon2id блокирует поток на десятки миллисекунд — так и задумано; завести отдельный `task_processor` и считать хеш на нём (`engine::AsyncNoSpan`) | Под нагрузкой входов задержки остальных ручек не растут | [authentication.md](authentication.md), `PDR-IDENT-02` |
| Ручки входа нет: транспорт написан, но его никто не зовёт | Ручки `POST /sign-in`, `POST /sign-out`, `GET` по ссылке приглашения; выдача cookie с атрибутами из `identity::http::transport`; арендатор берётся из адреса кабинета | Сквозной сценарий контура: вход, обращение с cookie, выход; заголовок вместо cookie даёт тот же ответ | [authentication.md](authentication.md), `PDR-IDENT-02` |
| Ни одной ручки нет: форма для них есть, наследников у неё нет | Первый хендлер — наследник `infrastructure::http::AuthorizedHandler`: `Wants()`, `About()`, `Run()` и схема тела файлом рядом; маршрут в статическом конфиге сервиса | Сквозной сценарий контура: тот же отказ приходит той же формой, что в `pdr_adapter_tests` | [http.md](http.md), `PDR-API-01` |
| Опознание пришедшего некому делать: порт есть, адаптера нет | Реализовать `infrastructure::http::Callers` в `identity`: `Where()` отдаёт имена из `identity::http::transport`, `Identify()` зовёт `ReadSessionId` и `AuthenticateSession` | Обращение с cookie доходит до сценария, с истёкшей — получает 401 и не доходит до политики | [http.md](http.md), [authentication.md](authentication.md), `PDR-IDENT-02` |
| Уборку просроченных ключей повтора никто не зовёт сам | Задание уборки по `expires_at`; запрос и индекс уже есть (`db/http/prune.sql`, `http_idempotency_key_by_age`). Задание работает под ролью приложения, а обойти все практики из-под построчной защиты нечем: нужен либо обход арендаторов из самого механизма, либо роль, которой защита не мешает, — и то и другое решается вместе с первым сервисом | Прогон удаляет просроченные ключи и не трогает живые; `make idempotency-prune` уже делает это под ролью миграций | [idempotency.md](idempotency.md), [jobs.md](jobs.md) |
| Уборки истёкших сессий и отработавших окон нет — таблицы растут | Задание уборки по `expires_at` и `window_started_at`; индексы `identity_session_alive_by_person` и `identity_login_attempt_by_age` для него уже есть | Прогон удаляет истёкшее и не трогает живое | [authentication.md](authentication.md), [jobs.md](jobs.md) |
| Перебор одного пароля по многим кабинетам не ловится | Счётчики лежат под изоляцией и между кабинетами не складываются; такой перебор ловится на границе, а не в сервисе | Ограничение по адресу поверх всех кабинетов | [authentication.md](authentication.md) |
| Письма с приглашением и ссылкой сброса некому послать | Сценарии возвращают секрет тому, кто их позвал; доставка — контекст `notifications` | Приглашение доходит до почты, а ссылка в письме открывается один раз | [authentication.md](authentication.md), [context-map.md](context-map.md) |
| Причина отказа в правах никуда не отображается | Завести отображение `identity::DenyReason` в problem+json — в одном месте, вместе с остальными доменными ошибками; каждой причине написать русский текст в `clients/shared/i18n/ru.json` | `node scripts/check-copy.mjs` начнёт требовать текст сам, как только причина станет кодом отказа; сейчас требовать нечего — кода нет | [permissions.md](permissions.md), `PDR-IDENT-03` |
| Начало срока на решение объявлять некому: сценарий есть, зовущего нет | Завести периодическое задание контекста `identity`: обойти пары «опекун — ученик», у которых пошёл срок, и позвать `AnnounceGuardianHandover`; уведомление обеим сторонам — подписчик на `identity.guardian_handover_started` в `notifications` | Задание объявляет один раз на пару и молчит вне срока; `HandoverTest` уже проверяет вторую половину | [guardian-access.md](../runbooks/guardian-access.md), [jobs.md](jobs.md), `PDR-IDENT-04` |
| Дата рождения есть не у всех: у людей, заведённых до `PDR-IDENT-04`, колонка пуста | Спросить дату при следующем входе и заполнить; пока пусто — правило совершеннолетия не срабатывает и доступ опекуна остаётся | Доля пустых `identity_person.born_on` видна метрикой и убывает | [guardian-access.md](../runbooks/guardian-access.md), `PDR-IDENT-04` |
| Переход через возрастной порог объявлять некому: сценарий есть, зовущего нет | Завести периодическое задание контекста `identity`: обойти учеников, у которых сегодня день рождения, и позвать `AnnounceComingOfAge`; письма обеим сторонам разошлёт подписчик на `identity.capabilities_widened`, он уже написан | Задание объявляет один раз в день перехода и молчит в остальные; `ComingOfAgeTest` уже проверяет вторую половину | [permissions.md](permissions.md), [jobs.md](jobs.md), `PDR-IDENT-05` |
| Уведомление опекуну о самостоятельном поступке некому вызвать: переноса, отмены и отзывов в дереве нет | Позвать `NotifyGuardianOfAct` из сценариев переноса и отмены занятия (`scheduling`, их ещё нет — есть только запись) и из сценария отзыва (контекста отзывов нет вовсе) | `scripts/check_guardian_notice.py` уже требует, чтобы у выключателя не было места; добавить в него требование вызова, как только вызывающий появится | [permissions.md](permissions.md), `PDR-IDENT-05` |
| Пароль живёт в практике, а не в учётной записи: у человека с двумя репетиторами два пароля | Перенести `identity_credential` и вход на `identity_account`: одна запись — один пароль. Задевает `SignIn`, `ChangePassword`, `ResetPassword`, `AcceptInvitation` и свою миграцию | Человек, приглашённый вторым репетитором, входит прежним паролем; `OnboardingTest` уже проверяет, что человек при этом один | [ADR-0019](../adr/0019-one-account-many-practices.md), `PDR-IDENT-06` |
| Учётная запись остаётся после удаления последней практики | Убирать `identity_account`, когда у человека не осталось ни одной связи: сегодня удаление практики её не трогает намеренно — у ученика может быть второй репетитор | После удаления единственной практики человека в реестре нет; после удаления одной из двух — есть | [ADR-0019](../adr/0019-one-account-many-practices.md), [onboarding.md](onboarding.md) |
| Приглашения и ссылки подтверждения никто не отправляет | Подписчик в `notifications` на выданные приглашения и на просьбу подтвердить почту; сегодня сценарии возвращают секрет тому, кто их позвал | Двадцать приглашений доходят до двадцати ящиков, повторная отправка не добавляет ни одного письма | [onboarding.md](onboarding.md), `PDR-IDENT-06` |
| Уборки счётчика заведений нет — таблица растёт | Задание уборки по `window_started_at`; индекс `identity_signup_attempt_by_age` для него уже есть | Прогон удаляет отработавшие окна и не трогает свежие | [onboarding.md](onboarding.md), [jobs.md](jobs.md) |
| Поломка настройки прав видна только в журнале | Поднять `no_policy` до метрики: одна незаведённая политика — это тихо запрещённое всем действие, и ждать жалобы неделю нельзя | Счётчик `utils::statistics` растёт, тревога срабатывает на первом же случае | [permissions.md](permissions.md), `PDR-IDENT-03` |
| Разворачивающей команды нет | Область `INF`: команда появляется сразу в рабочем виде, а не «пока вручную» | Локальная установка поднимает сервис одной командой | [ADR-0007](../adr/0007-local-without-fake-production.md) |

## Дырка в контракте заданий, которую придётся закрыть

`jobs::WorkItem` требует арендатора:

```cpp
struct WorkItem final {
    core::TenantId tenant;
    std::string key;
};
```

Так сделано намеренно: идемпотентность лежит в `jobs_effect (tenant_id, job,
effect_key)`, а действие всегда чьё-то. **Но кластерная работа арендатора не
имеет вовсе:** уборка просроченных строк `jobs_lock`, сверка, пересчёт горизонта
для всех сразу. Сегодня такое задание в контракте не выражается — и это
обнаружилось при попытке завести первое настоящее задание, а не при написании
контракта.

Два выхода, выбор делается в `PDR-SCHED-04` и записывается ADR:

1. **Задание объявляет, нужен ли ему след.** `PeriodicJob` получает признак «моя
   работа кластерная»; для таких заданий механизм не ставит след и не спрашивает
   арендатора. Дешевле, но идемпотентность кластерных заданий становится их
   собственной заботой.
2. **Арендатор системы.** Отдельная строка в `identity_tenant` для работ,
   которые не принадлежат никому. Контракт не меняется, `jobs_effect` работает
   как раньше, RLS не делает исключений. Дороже на одну строку в схеме, зато
   механизм остаётся один.

Второй вариант выглядит правильнее, но решать при живом задании, а не сейчас.

## Чего стоит ожидать от сборки

Проверено на машине владельца, чтобы это не выяснялось в момент старта задачи:

* **системный AppleClang не собирает userver.** Его собственные заголовки не
  компилируются: `userver/utils/zstring_view.hpp:55: invalid operands to binary
  expression` — из-за `operator<=>` в libc++ Apple. Локальная сборка требует
  другого компилятора;
* **рабочий путь — контейнер** `ghcr.io/userver-framework/ubuntu-22.04-userver`:
  2.2 ГБ, 20 слоёв, userver внутри уже собран. В нём же гоняется контур;
* **локальный чекаут userver сконфигурирован с `USERVER_FEATURE_POSTGRESQL=OFF`** —
  для сервиса нужен `ON`, то есть полная пересборка;
* адаптеры в дереве (`pdr_postgres`, `pdr_jobs_infrastructure`,
  `pdr_billing_infrastructure`) **собираются** — джоба `adapters-with-userver`
  компилирует их в контейнере с настоящим userver на каждый push. Чего они ещё не
  делали, так это не запускались: цели статические, символы разрешаются при
  линковке сервиса. Первый запуск — за `PDR-SCHED-04`;
* **`find_package(userver)` зовётся ровно один раз, в корневом
  `CMakeLists.txt`.** Второй вызов в другом каталоге ломает сборку на этапе
  генерации: импортированные цели видны только в своём каталоге, а `Boost_FOUND`
  кэшируется глобально, поэтому `find_dependency` пропускается как «уже найдено»
  и ссылка `Boost::stacktrace_backtrace` в интерфейсе `userver::core` оказывается
  битой. Сервис добавляет свои цели, а не свой `find_package`.

## Что первый сервис НЕ должен подбирать

Долг — не повод расширять задачу. В `PDR-SCHED-04` не входит:

* доменные правила в `services/` — их место в `core`, и это проверяется
  ([layers.md](layers.md));
* ручки чужих контекстов: сервис поднимает `scheduling`, а не всё сразу
  ([context-map.md](context-map.md));
* кэш справочников, если к тому времени не появилось справочной таблицы: пустой
  кэш — это неиспользуемый каркас;
* георазбивка конфигов, мультивалютность, вторая тема оформления — записано в
  своих задачах как «не делаем».
