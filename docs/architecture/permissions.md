# Права: кто что может

**Проверка прав живёт в одном месте, и это не хендлер.** Хендлер спрашивает
`identity::Contract::Decide` и не знает содержимого ответа: новая роль или новое
правило — это новая политика внутри `identity`, а не правка двадцати хендлеров,
каждый из которых понял правило чуть-чуть по-своему.

Аутентификация — отдельный вопрос и отдельный документ
([authentication.md](authentication.md)). Там «кто это», здесь «что ему можно».

## Решение — не «да» и «нет»

`PolicyDecision` несёт причину, и это не украшение. Человеку нужно понять, что
делать дальше: «это не ваш кабинет» и «этот ученик не ваш» приводят к разным
следующим шагам. А разработчику при разборе жалобы нужно отличать отказ по
правилу от отказа из-за незаведённой политики — по слову «нельзя» это не
отличается никак.

| Причина | Что произошло | Что делать человеку |
| --- | --- | --- |
| `allowed` | отказа нет | — |
| `foreign_tenant` | ресурс из чужого кабинета | проверить, туда ли он зашёл |
| `role_missing` | роли, которой такое даётся, у него нет вовсе | попросить репетитора выдать роль |
| `not_yours` | роль подходящая, но ресурс чужой | ничего: это не его ученик и не его занятие |
| `scope_missing` | он опекун этого ученика, но **этот уровень** ему не открывали | попросить открыть уровень: репетитора или самого ученика |
| `student_grew_up` | ученик стал взрослым, и слово теперь за ним | попросить ученика — больше это не может никто |
| `no_policy` | **поломка настройки**: действие завели, а правило к нему нет | чинить, а не объяснять; отказ при этом уже произошёл |

**Значение по умолчанию — запрет.** Действие без политики не разрешается «пока»
и не запрещается молча: оно запрещается и сообщается как поломка
(`ports::ConfigurationFaults`). Тихое разрешение стоило бы ровно столько,
сколько стоит открытая дверь, о которой никто не знает.

## Отношение к ресурсу

Роли мало: «репетитор» и «репетитор ЭТОГО занятия» — разные вещи. Поэтому
решение принимается по паре «роль и отношение», а отношение считается заранее,
до политики, — «опекун ли он» это вопрос к хранилищу, а политика хранилищ не
знает и потому проверяется таблицей, а не поднятым сервисом.

| Отношение | Кто это |
| --- | --- |
| `своё` | он ведёт этот ресурс: репетитор занятия, выставивший счёт, автор материала |
| `о себе` | ресурс о нём самом |
| `о подопечном` | ресурс о том, кого он опекает |
| `чужое` | никем: ресурс кабинета вообще или чужой |

У занятия есть и ведущий репетитор, и ученик, поэтому одному человеку могут
подходить сразу два отношения. Берётся ближайшее — в порядке этой таблицы
сверху вниз.

## Уровни опекуна

**Единого «родитель видит всё» не существует.** У опекуна четыре уровня, каждый
открывается отдельной строкой согласия, и колонка «Уровень опекуна» в матрице
ниже говорит, каким уровнем открыто каждое действие.

| Уровень | Что открывает | Даётся вместе с опекой |
| --- | --- | --- |
| `schedule` | записать, отменить, перенести, смотреть расписание | да |
| `payments` | смотреть и оплачивать счета | да |
| `notes_and_homework` | материалы, что получается, выгрузка истории | да |
| `recordings` | слушать запись занятия, читать расшифровку | **нет** |

Записи занятий не открываются вместе с опекой намеренно: запись урока — это
голос ребёнка и голос репетитора, и согласие на неё спрашивают отдельно. Человек,
нажавший «подключить родителя», про запись не думал.

**Совершеннолетие не обрывает доступ в день рождения.** Оно начинает срок на
решение (`PDR_GUARDIAN_HANDOVER_DAYS`): всё это время доступ работает
по-прежнему, а после срока `notes_and_homework` и `recordings` закрываются, пока
ученик не откроет их сам. `schedule` и `payments` не закрываются — за занятия
платит родитель. Согласие, которое ученик дал сам, совершеннолетие не трогает
вовсе: своё слово он уже сказал.

Порядок действий по обращению «закройте доступ» и сроки удаления данных —
[runbooks/guardian-access.md](../runbooks/guardian-access.md).

## Матрица

**Она собрана из самих политик, а не написана руками.** Каждое действие
спрошено у набора за каждую роль и за каждое отношение, и в клетку попало то,
что политика ответила. Матрица, написанная руками, расходится с кодом на первой
правке и после этого хуже, чем её отсутствие: по ней принимают решения, а она
врёт.

Сверяет её тест `libs/pdr-identity/tests/permissions_matrix_test.cpp`,
перезаписывает — `make permissions-lock`. Правка руками не переживёт `make test`.

<!-- матрица прав: начало -->

| Действие | Код | Владелец школы | Репетитор | Ученик | Опекун | Уровень опекуна |
| --- | --- | --- | --- | --- | --- | --- |
| Записать занятие | `book_lesson` | — | своё | о себе | о подопечном | `schedule` |
| Отменить занятие | `cancel_lesson` | — | своё | о себе | о подопечном | `schedule` |
| Перенести занятие | `reschedule_lesson` | — | своё | о себе | о подопечном | `schedule` |
| Смотреть расписание | `view_schedule` | любое в кабинете | своё | о себе | о подопечном | `schedule` |
| Смотреть счёт | `view_invoice` | любое в кабинете | своё | о себе | о подопечном | `payments` |
| Оплатить счёт | `pay_invoice` | — | — | о себе | о подопечном | `payments` |
| Вернуть деньги | `issue_refund` | любое в кабинете | своё | — | — | — |
| Назначить цену | `set_tariff` | любое в кабинете | своё | — | — | — |
| Открыть материал | `view_material` | — | своё | о себе | о подопечном | `notes_and_homework` |
| Править материал | `edit_material` | — | своё | — | — | — |
| Опубликовать материал | `publish_material` | — | своё | — | — | — |
| Назначить программу занятий | `assign_plan` | — | своё | — | — | — |
| Смотреть, что получается | `view_progress` | — | своё | о себе | о подопечном | `notes_and_homework` |
| Записать попытку | `record_attempt` | — | — | о себе | — | — |
| Унести историю занятий | `export_progress` | — | — | о себе | о подопечном | `notes_and_homework` |
| Смотреть сводку по кабинету | `view_tenant_progress` | любое в кабинете | — | — | — | — |
| Слушать запись занятия | `view_lesson_recording` | — | своё | о себе | о подопечном | `recordings` |
| Читать расшифровку занятия | `view_lesson_transcript` | — | своё | о себе | о подопечном | `recordings` |
| Смотреть, кто заходил в мои данные | `view_access_journal` | — | своё | о себе | — | — |
| Открывать и отзывать доступ опекуну | `manage_guardian_access` | — | своё | о себе | — | — |
| Написать отзыв о репетиторе | `write_review` | — | — | о себе | — | — |
| Подключить списание с карты | `manage_auto_payment` | — | — | — | о подопечном | `payments` |
| Позвать людей в практику | `invite_people` | любое в кабинете | любое в кабинете | — | — | — |
| Распоряжаться практикой | `manage_practice` | любое в кабинете | — | — | — | — |

<!-- матрица прав: конец -->

## Возраст × действие

**Права приходят сами, а не по заявке.** Никто не выдаёт подростку
самостоятельность и никто не подаёт заявки: возможности считаются из даты
рождения и порогов конфига на каждый вопрос, и в полночь дня рождения ответ
меняется без чьего-либо участия. Хранимого поля «может платить» нет и не будет —
оно устарело бы к утру.

Отдельной роли «взрослый ребёнок» тоже нет. Это тот же `student` с другим
набором возможностей: новая роль размножила бы политики и разъехалась бы с
опекой, потому что роль выдают руками, а возраст приходит сам.

Таблица собрана тем же опросом, что и первая: ученику и опекуну задаётся одно и
то же действие при возрасте каждой полосы, и в клетку попадает то, кому политика
ответила «да». Пороги в заголовке — умолчания реестра, а не константы кода.

Читается она при одном допущении: **все уровни опекуну открыты**. Какие из них
открываются вместе с опекой, а какие требуют отдельного согласия, говорит
таблица выше — записи занятий сами не открываются никогда. И наоборот: клетка
«—» у последней строки означает «по умолчанию никто», а не «нельзя вовсе»;
совершеннолетний ученик может открыть уровень родителю сам, и тогда тот вернётся.

<!-- матрица возраста: начало -->

| Действие | Код | младше 14 | с 14 | с 16 | с 18 |
| --- | --- | --- | --- | --- | --- |
| Записать занятие | `book_lesson` | опекун | опекун | оба | ученик |
| Отменить занятие | `cancel_lesson` | опекун | оба | оба | ученик |
| Перенести занятие | `reschedule_lesson` | опекун | оба | оба | ученик |
| Смотреть расписание | `view_schedule` | оба | оба | оба | ученик |
| Смотреть счёт | `view_invoice` | оба | оба | оба | ученик |
| Оплатить счёт | `pay_invoice` | опекун | опекун | оба | ученик |
| Открыть материал | `view_material` | оба | ученик | ученик | ученик |
| Смотреть, что получается | `view_progress` | оба | ученик | ученик | ученик |
| Записать попытку | `record_attempt` | ученик | ученик | ученик | ученик |
| Унести историю занятий | `export_progress` | оба | ученик | ученик | ученик |
| Слушать запись занятия | `view_lesson_recording` | оба | ученик | ученик | ученик |
| Читать расшифровку занятия | `view_lesson_transcript` | оба | ученик | ученик | ученик |
| Смотреть, кто заходил в мои данные | `view_access_journal` | ученик | ученик | ученик | ученик |
| Открывать и отзывать доступ опекуну | `manage_guardian_access` | — | ученик | ученик | ученик |
| Написать отзыв о репетиторе | `write_review` | — | ученик | ученик | ученик |
| Подключить списание с карты | `manage_auto_payment` | опекун | опекун | опекун | — |

<!-- матрица возраста: конец -->

Что видно в этой таблице и не видно в первой: **у одного и того же действия
ответ меняется со временем**. Записаться к репетитору за подростка сначала
может только опекун, потом оба, потом только он сам; расписание уходит из рук
опекуна ровно на совершеннолетии, а конспекты и записи — на первом пороге.

Действий про саму практику в этой таблице нет: «своё занятие» бывает, «своя
практика» — нет. Звать людей может владелец и репетитор — это ежедневная работа,
и гонять за ней к хозяину значит сделать перенос практики чужой заботой.
Распоряжаться практикой — видимостью в подборе, выгрузкой и удалением — может
только владелец ([onboarding.md](onboarding.md)).

Три вещи, которых в таблице нет намеренно:

* **автоплатёж с карты опекуна учеником не управляется ни в каком возрасте.**
  «Ученик платит сам» — это его собственные средства; привязанная карта родителя
  остаётся родительской. Сложи их в одно право — и шестнадцатилетний
  распорядится чужой картой, а заметят это на первом списании;
* **уведомление опекуну о самостоятельном поступке не отключается.** Подросток
  переносит занятие без разрешения, а родитель узнаёт об этом фактом, а не
  расследованием: смысл порога — самостоятельность, а не тайна. Выключателя нет
  нигде в дереве, и это проверяет `scripts/check_guardian_notice.py`;
* **текста отзыва опекун не видит.** Он видит, что отзыв написан. Право
  высказаться без надзора и есть содержание порога, поэтому в событии
  `identity.ward_acted_alone` нет поля, в которое текст поместился бы.

Сами числа — **вопрос права, а не продукта**, и до публичного запуска их обязан
посмотреть юрист ([ADR-0018](../adr/0018-age-thresholds-are-a-legal-question.md)).

## Чего здесь нет

Написано честно, чтобы никто не рассчитывал на несуществующее:

* **супер-администратора не существует.** Владелец школы ведёт школу: видит
  расписание, распоряжается ценами и возвратами, смотрит сводку. Записать,
  отменить или оплатить за человека он не может — «владелец может всё» это
  самая дорогая строка в любой системе прав, потому что после неё все остальные
  строки становятся необязательными;
* **правила времени сюда не входят.** «Окно отмены истекло» — это правило
  расписания, а не прав: оно зависит от часов и от самого занятия, а политика
  не знает ни того, ни другого. Права отвечают «кому можно», расписание —
  «когда ещё можно», и оба ответа нужны;
* **отдельной таблицы дееспособности нет и не будет.** Возраст считается от
  `identity_person.born_on` и момента, а не хранится числом, которое врёт со
  следующего дня рождения. Порог живёт в `PDR_SELF_ACCOUNT_AGE`, срок на
  решение — в `PDR_GUARDIAN_HANDOVER_DAYS`. Дата рождения при этом может быть
  неизвестна: тогда правило совершеннолетия не срабатывает вовсе;
* **хендлеров нет вовсе** — как и всего слоя HTTP. Отображение отказа в
  problem+json появится вместе с первым сервисом
  ([first-service.md](first-service.md));
* **записи в журнал доступа политика не делает.** Право смотреть и право
  смотреть незаметно — разные вещи, и вторым занимается
  `identity::NoteSensitiveAccess` ([tenancy.md](tenancy.md)). Зовёт его
  `ContractService::Decide` — тот же вызов, который решает права, поэтому
  посмотреть содержание занятия, не оставив следа, нечем.
