# ADR-NNNN. Интеграция: <название внешнего сервиса>

<!-- Шаблон ADR интеграции (PDR-ARCH-04). Копируется под своим номером, все
     угловые скобки заполняются. Раздел «Как это выключить» обязателен и
     проверяется scripts/check_integrations.py: интеграция без него не
     принимается. Правило целиком — ADR-0014. -->

**Статус:** Принято, ГГГГ-ММ-ДД.

## Контекст

<Что за сервис, какую функцию продукта он закрывает и почему эту функцию не
сделать самим. Если её можно сделать самим — интеграция не нужна.>

## Решение

**Официальность:** <официальный | неофициальный>.

<Официальный — есть публичный API с обязательствами по совместимости и
объявленным порядком изменений. Неофициальный — всё остальное, включая
реверс-инженеренные библиотеки. Неофициальный несущим не бывает: ADR-0014.>

**Роль:** <несущая | украшение>.

<Украшение — продукт работает без этой функции, человек видит объяснение.
Несущая — функции нет совсем, и это допустимо только для официального API.>

**Порт:** `<путь к заголовку порта в application/ports/>`.

**Реализации:** `<настоящая>` и `<заглушка>`.

**Флаг отключения:** `PDR_INTEGRATIONS.<имя>` — динамический конфиг, не правка
кода.

**Таблица соответствия:** `<имя таблицы>` — здесь и только здесь живёт чужой
идентификатор. Первичными ключами наших сущностей чужие значения не бывают.

## Как это выключить

<!-- Раздел обязателен. Ответ «неизвестно» означает, что интеграцию делать
     рано: цена удаления неизвестна, а именно она и убила интеграцию в six-feat
     (ADR-0014). -->

**Осиротеют таблицы:** <перечислить поимённо; «ни одна» — тоже ответ>.

**Исчезнут функции:** <что перестанет быть доступно человеку и что он увидит
вместо этого>.

**Затронуто файлов:** <число и как оно получено: `git grep -l` по имени порта,
адаптера и таблицы>.

**Что останется:** <данные, которые нужно сохранить или удалить, и почему>.

## Последствия

<Чем платим: задержка, стоимость, чужие сроки, персональные данные, уходящие
наружу. Неприятное пишется здесь, а не выясняется потом.>

## Альтернативы и почему отвергнуты

**<Сделать самим.>** <Почему нет — или почему да, и тогда интеграция не нужна.>

**<Другой провайдер той же функции.>** <Почему не он.>

**<Не делать эту функцию вовсе.>** <Отвергнуто или принято — но рассмотрено
обязательно: у украшения это всегда живая альтернатива.>
