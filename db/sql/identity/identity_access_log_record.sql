-- @no-dto
-- Приведение ::uuid написано явно. Идентификатор уезжает текстом — тем же
-- ToString(), что и всюду, — а колонка типизирована; полагаться на то, что база
-- сама догадается привести текст к uuid при вставке, здесь не за чем:
-- написанное приведение видно, а угаданное — нет.
--
-- Момент приходит параметром, хотя у колонки есть default now(): часы у нас
-- портом, и строка «кто смотрел в марте» должна отвечать по тем же часам, что и
-- весь остальной сценарий, а не по вторым, базы.
--
-- Строк не возвращает; разобрать запрос разборщик к тому же не может —
-- образцовый resource_kind не проходит identity_access_log_kind_known.
INSERT INTO identity_access_log
            (tenant_id, id, actor_id, subject_id, resource_kind, at)
     VALUES ($1::uuid, $2::uuid, $3::uuid, $4::uuid, $5, $6)
