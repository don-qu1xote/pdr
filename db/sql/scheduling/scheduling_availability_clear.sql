-- @no-dto
-- Строк не возвращает. Доступность записывается целиком: снести и положить
-- заново внутри одной транзакции — это ровно та операция, которую делает
-- репетитор на экране, и она атомарна.
DELETE FROM scheduling_availability
 WHERE tenant_id = $1::uuid
   AND tutor_id = $2::uuid
