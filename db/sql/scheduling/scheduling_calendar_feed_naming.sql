-- @no-dto
-- Строк не возвращает.
--
-- Способ называть занятия меняется ОТДЕЛЬНО от секрета: «показывать имена» —
-- настройка, а перевыпуск — починка утечки, и делать их одним действием значило
-- бы менять ссылку у того, кто пришёл поправить настройку.
UPDATE scheduling_calendar_feed
   SET naming = $3
 WHERE tenant_id = $1::uuid
   AND person_id = $2::uuid
