-- @no-dto
-- Строк не возвращает.
--
-- НОВОЕ ПРАВИЛО У ТОЙ ЖЕ СЕРИИ: та же строка, тот же идентификатор. Серия,
-- отодвинутая за отпуск, кончается днём перед ним, а остаток уезжает новой
-- серией; прежнюю при этом переписывают, а не заводят заново — занятия,
-- которые она уже провела, ссылаются на неё.
--
-- Участники не трогаются: сдвиг — это про время, а не про состав.
UPDATE scheduling_series
   SET rrule = $3,
       starts_on = $4::date
 WHERE tenant_id = $1::uuid
   AND id = $2::uuid
