#pragma once

#include "core/digest.hpp"

namespace pdr::identity {

/// Отпечаток — платформенный тип (`core::Digest`), а не свой.
///
/// Здесь он был, пока секретные ссылки выдавало только опознание. Подписка на
/// календарь — такая же ссылка и такой же отпечаток, а включать заголовок
/// identity в расписание нельзя; поэтому тип переехал в платформу, а имя
/// осталось на месте: у identity его зовут `identity::Digest` полтора десятка
/// файлов, и переименование ради переезда ничего бы не улучшило.
using Digest = core::Digest;

}  // namespace pdr::identity
