{
  "_": [
    "Файл секретов ДЛЯ КОНТУРА, а не для установки. Значения заведомо",
    "ненастоящие: рабочий файл в историю не попадает и лежит с правами 0600",
    "(deploy/secrets/secrets.json.example).",
    "",
    "Он здесь затем, чтобы контур проверял НАСТОЯЩИЙ путь старта: секреты",
    "читаются, VerifySecrets отрабатывает, и отсутствие обязательного секрета",
    "роняет сервис так же, как уронило бы в установке.",
    "",
    "Строка подключения при этом не используется: pytest_userver подменяет",
    "dbconnection адресом своей базы (см. conftest.py)."
  ],

  "pdr": {
    "postgres_dsn": "postgresql://pdr:not-a-secret@127.0.0.1:5432/pdr"
  },

  "postgresql_settings": {
    "databases": {
      "pdr": [
        {
          "shard_number": 0,
          "hosts": ["postgresql://pdr:not-a-secret@127.0.0.1:5432/pdr"]
        }
      ]
    }
  }
}
